# MVP1 Backend

- Endpoints: auth/login, users, customers, suppliers, items, accounts, journal
- Uses MySQL (ERP2) and simple JWT-based auth
- Plaintext passwords for seeds (to be hashed in production)
- Run: npm install, then npm run dev (or node src/index.js)
